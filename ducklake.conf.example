# DuckLake Configuration File
# Copy this file to ducklake.conf and modify as needed
# Use with: ./ducklake-installer.sh --config ducklake.conf

# Installation directory (absolute or relative path)
INSTALL_DIR=./ducklake

# Instance name for this DuckLake setup (useful for multiple instances)
INSTANCE_NAME=default

# S3/MinIO bucket configuration
BUCKET_NAME=ducklake-bucket
DATA_PATH=data

# PostgreSQL catalog database configuration
DB_NAME=ducklake_catalog
DB_USER=ducklake
DB_PASS=ducklake123

# Port configuration (ensure these don't conflict with existing services)
POSTGRES_PORT=5432
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# Example configurations for different scenarios:

# Development setup:
# INSTALL_DIR=./ducklake-dev
# INSTANCE_NAME=dev
# POSTGRES_PORT=5433
# MINIO_PORT=9001
# MINIO_CONSOLE_PORT=9002

# Production setup:
# INSTALL_DIR=/opt/ducklake
# INSTANCE_NAME=prod
# DB_PASS=your_secure_password_here
# POSTGRES_PORT=5432
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001